import 'package:flutter/material.dart';

class PLLAlgorithmBody extends StatelessWidget {
  const PLLAlgorithmBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PLL Algorithm Screen'),
      ),
    );
  }
}